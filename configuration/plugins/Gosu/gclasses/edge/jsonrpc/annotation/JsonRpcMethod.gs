package edge.jsonrpc.annotation

/**
 * Used to annotate methods in our handlers as methods used for processing JSON RPC requests
 */
final class JsonRpcMethod implements IAnnotation{
  construct() {}
}
